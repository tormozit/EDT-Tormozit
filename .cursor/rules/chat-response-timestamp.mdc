---
description: В начале финального текстового ответа агента — метка времени DD-HH:MM:SS
alwaysApply: true
---

# Метка времени в ответах чата

В **самом начале финального текстового ответа** ассистента пользователю выводи актуальные дату и время в формате `DD-HH:MM:SS` **на той же строке**, что и основной текст ответа.

- **DD** — номер дня месяца, две цифры с ведущим нулём (`01` … `31`).
- **HH:MM:SS** — часы, минуты, секунды в 24-часовом формате, по две цифры.

**Правильно** (одна строка, метка + пробел + текст):

```text
12-19:35:28 Правило обновлено…
```

**Неправильно** — метка на отдельной строке, без текста ответа:

```text
12-19:35:28

Правило обновлено…
```

## Когда ставить

- **Да** — только в **финальном** сообщении ассистента пользователю: обычный текст на человеческом языке (объяснение, отчёт, ответ на вопрос).

## Когда не ставить

- В аргументах и тексте вызовов инструментов как пояснение для пользователя.
- В чисто техническом выводе без пояснения для человека.

## Как брать время

- **Обязательно** — актуальное **локальное** время машины пользователя.
- **Запрещено** вызывать Shell **только** ради метки (отдельный `Get-Date` / `date` пользователь видит в чате).
- Если в том же turn уже нужен Shell для задачи — добавь получение времени **в конец той же команды** (Windows: `…; powershell -NoProfile -Command "Get-Date -Format 'dd-HH:mm:ss'"`; Linux/macOS: `…; date +%d-%H:%M:%S`) и возьми метку из вывода.
- Если Shell иначе не нужен — метку **не запрашивай** отдельным вызовом; подставь время без демонстрации команды пользователю.
- **Запрещено** в тексте ответа пользователю:
  - упоминать, что время запрашивалось;
  - цитировать команду или её вывод;
  - показывать/описывать, как получена метка;
  - писать «сейчас примерно…», «оценка…» вместо метки.
- В финальном сообщении — только готовая метка в первой строке, без пояснений про её получение.

## Оформление

- **Обязательно**: `DD-HH:MM:SS` + пробел + первое слово ответа — всё на **одной** первой строке.
- Метка не может быть единственным содержимым первой строки; сразу после неё идёт текст для пользователя.
- После метки — один пробел, затем основной текст (без префикса «Время:», без markdown-заголовка `##`).
- Абзацы и списки начинай со **второй** строки; метку не повторяй.
- Не дублируй метку в середине или в конце ответа.
