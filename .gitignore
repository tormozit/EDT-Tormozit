.tmp*
# PDE update site build output (не коммитить — пересобирается локально)
site/features/
site/plugins/
site/content.jar
site/artifacts.jar
site/*.zip

# Maven/Tycho build output (but not the root /target which is the target-platform project)
**/target/
!/target/
